import { SuperherFetchJsonPipe } from './superher-fetch-json.pipe';

describe('SuperherFetchJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new SuperherFetchJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
