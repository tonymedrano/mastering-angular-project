import { FlyingSuperheroesPipe } from './flying-superheroes.pipe';

describe('FlyingSuperheroesPipe', () => {
  it('create an instance', () => {
    const pipe = new FlyingSuperheroesPipe();
    expect(pipe).toBeTruthy();
  });
});
